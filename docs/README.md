# Screenshots

Drop real captures here so they render in the root `README.md`. Expected files:

| File                        | Suggested capture                                              |
| --------------------------- | -------------------------------------------------------------- |
| `screenshot-landing.png`    | The marketing home hero (`/`)                                  |
| `screenshot-chat.png`       | The chat widget mid-qualification, ideally showing a booking card |
| `screenshot-crm.png`        | The `/crm` dashboard with KPI cards + the leads table          |
| `screenshot-analytics.png`  | The `/analytics` page showing the conversion funnel + chart    |

Recommended: 1440×900 viewport, dark mode (the app is dark-only), PNG.
